syntax = "proto3";

option go_package = "api/proto";

package api;

service Api {
  rpc GetHello(Request) returns (Reply) {}
}

message Request {
  string name = 1;
}

message Reply {
  string message = 1;
}